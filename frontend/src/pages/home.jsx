import React from "react";

export default function Home() {
  return (
    <div>
      <h1>Stock Predictor</h1>
      <p>FastAPI ML service + Express gateway + MongoDB + Redis + React frontend.</p>
      <p>Go to Predict to run a prediction, or Results to see saved predictions.</p>
    </div>
  );
}
